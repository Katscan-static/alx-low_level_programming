#include <stdio.h>

/**
 * main - print program name
 * @argc: number of variables
 * @argv: argument variables
 * Return: Always (0)
 */

int main(int argc, char **argv)
{
	argc = 0;
	printf("%s\n", argv[argc]);
	return (0);
}
